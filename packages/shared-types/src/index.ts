// Types generated from the FastAPI OpenAPI schema (ARCHITECTURE.md §18),
// shared between apps/web and apps/api's documented API surface. Nothing
// worth generating yet in Phase 0 — the first real endpoints (repo
// connect/sync) land in Phase 0 PR3, at which point this becomes an
// `openapi-typescript` generation target rather than a hand-written file.
export {};
