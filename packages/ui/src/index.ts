// Blueprint's shared design system. The first real export — FindingCard,
// the single component every Finding type renders through (RULES.md §5) —
// ships in Phase 1 alongside the findings schema it renders. Nothing to
// export yet in Phase 0.
export {};
